/**
 * Represents a flag entity with a name.
 * Extends the Entity class.
 */
package entitys;

public class Flag extends Entity {

    /**
     * Creates a new flag with the specified name.
     *
     * @param name The name of the flag.
     */
    public Flag(String name) {
        super(name);
    }
}
